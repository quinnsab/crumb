<%- include('partials/header') -%>
  <div class="container">
    <div class="row justify-content-center mt-5">
    
      <ul class="row list-unstyled">
        <% for(var i=0; i<posts.length; i++) {%>
          <li class="col-6 justify-content-between mt-5">
            <div class="card" style="width: 18rem;">
            <a class="card-img-top" href="/post/<%= posts[i]._id%>">
              <img class="img-fluid shadow-lg" src="<%= posts[i].image%>">
            </a>
            <div class="card-body">
              <p class="card-text">[need desc field]</p>
            </div>
             </div>
          </li>
        <% } %>
        </ul>
    
    </div>
  </div>  
  <div class="container">
  <div class="row justify-content-center mt-6">
    <a class="btn btn-secondary btn-lg" href="/profile">Return to Profile</a>
  </div>  
</div>
<%- include('partials/footer') -%>

